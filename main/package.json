{
    "dependencies": {
        "bloatless-react": "^1.3.10",
        "esbuild": "latest"
    },
    "scripts": {
        "build": "esbuild src/index.tsx --bundle --loader:.png=file --loader:.jpg=file --outdir=dist"
    }
}
